name: noop - sync-to-melsec_mc (test branch)

on:
  push:
    branches: [ tmp/fix-sync-workflow ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: 'echo "noop: tmp/fix-sync-workflow - parsing fixed"'
          
